# db/

SQLite schema, migrations, repositories (CRUD only, no business logic).
Built in A2 per README §7.1. SQLite via `@capacitor-community/sqlite`, IndexedDB fallback on web.
All amounts are integer centavos.
